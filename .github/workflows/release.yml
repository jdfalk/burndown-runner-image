# file: .github/workflows/release.yml
# version: 2.3.0
# guid: f0c1ker0-0000-4000-8000-000000000003

name: Build image

on:
  push:
    branches: [main]
    tags: ['v*']
    paths-ignore:
      - '**.md'
      - 'docs/**'
  workflow_dispatch:
    inputs:
      stable:
        description: 'Publish as stable release (not pre-release)'
        required: false
        default: 'false'
        type: string

concurrency:
  group: build-image-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: write
  actions: write
  checks: write
  packages: write
  id-token: write
  attestations: write

jobs:
  release:
    name: Build + push image
    uses: jdfalk/ghcommon/.github/workflows/reusable-release.yml@ed36e5bb1774a3593d741555a7584ca260a18720 # fix GH_TOKEN + env-bridge guard
    with:
      release-type: 'auto'
      prerelease: ${{ !startsWith(github.ref, 'refs/tags/v') && inputs.stable != 'true' }}
      stable: ${{ startsWith(github.ref, 'refs/tags/v') || inputs.stable == 'true' }}
      draft: false
      docker-enabled: true
      build-target: 'docker'
    secrets: inherit
